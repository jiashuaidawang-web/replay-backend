<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dunwugudao.replay.mapper.ck.LeaderPoolDailyMapper">

    <insert id="insertBatch">
        INSERT INTO leader_pool_daily
            (trade_date, ts_code, board_code, board_pos, role, score, cat, amount, limit_style, note)
        VALUES
        <foreach collection="list" item="e" separator=",">
            (#{e.tradeDate}, #{e.tsCode}, #{e.boardCode}, #{e.boardPos}, #{e.role}, #{e.score},
             #{e.cat}, #{e.amount, jdbcType=DECIMAL}, #{e.limitStyle}, #{e.note})
        </foreach>
    </insert>

    <select id="selectByTradeDate" resultType="com.dunwugudao.replay.entity.LeaderPoolDaily">
        SELECT trade_date, ts_code, board_code, board_pos, role, score, cat, amount, limit_style, note
        FROM leader_pool_daily FINAL
        WHERE trade_date = #{tradeDate}
        ORDER BY score DESC
    </select>

    <select id="selectByTradeDateAndBoard" resultType="com.dunwugudao.replay.entity.LeaderPoolDaily">
        SELECT trade_date, ts_code, board_code, board_pos, role, score, cat, amount, limit_style, note
        FROM leader_pool_daily FINAL
        WHERE trade_date = #{tradeDate} AND board_code = #{boardCode}
        ORDER BY score DESC
    </select>

</mapper>
