<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dunwugudao.replay.mapper.ck.ThemeFactorDailyMapper">

    <insert id="insertBatch">
        INSERT INTO theme_factor_daily
            (trade_date, board_code, scarcity, imagination, sudden, certainty, min_resist, total)
        VALUES
        <foreach collection="list" item="e" separator=",">
            (#{e.tradeDate}, #{e.boardCode}, #{e.scarcity}, #{e.imagination}, #{e.sudden},
             #{e.certainty}, #{e.minResist}, #{e.total})
        </foreach>
    </insert>

    <select id="selectByTradeDate" resultType="com.dunwugudao.replay.entity.ThemeFactorDaily">
        SELECT trade_date, board_code, scarcity, imagination, sudden, certainty, min_resist, total
        FROM theme_factor_daily FINAL
        WHERE trade_date = #{tradeDate}
        ORDER BY total DESC
    </select>

    <select id="selectByTradeDateAndBoard" resultType="com.dunwugudao.replay.entity.ThemeFactorDaily">
        SELECT trade_date, board_code, scarcity, imagination, sudden, certainty, min_resist, total
        FROM theme_factor_daily FINAL
        WHERE trade_date = #{tradeDate} AND board_code = #{boardCode}
    </select>

</mapper>
