# 盘中实时模拟盘方案（顿悟股道 × 实时 L2 闭环）

> 文档定位：在现有 `replay-backend`（盘后复盘 S1–S8）基础上，扩展出一套**盘前定方向 → 盘中 L2 实时决策 → 盘后结算 → 经验反哺**的模拟盘闭环。
> 本文是**设计文档 + 表结构契约**，不动手写业务代码（表结构先行，代码后落）。
> 关键定位：本系统同时是未来**量化 agent 系统的数据底座**——所有模拟成交与经验反馈都以可被 agent 训练消费的可查询 schema 落库。

---

## 0. 设计原则（先立规矩）

1. **源无关（Source-agnostic）**：后端只接收 JSON 行情流，不关心 L2 来自同花顺 / 通达信 / 东财。换提供商只改 Python producer，后端零改动。
2. **传输与计算分离**（用户明确要求的标注）：
   - **Redis Streams = 仅传输 / 缓冲 / 解耦 / 可重放**（爬虫生产、后端消费之间的管道）；
   - **计算逻辑 = 后端自写内存滚动窗口**（大单净主动买入、封单、炸板回封），**不引入 Flink / Spark**。
3. **多口径统计优先**：每一笔模拟成交都带 `strategy_id × stage(入场情绪阶段) × capital_confirm(资金层口径) × direction × role`，后期按这些维度 GROUP BY 算胜率/盈亏，**由数据收窄策略**，而非预先写死"只做某阶段"。
4. **战法全量注册 + 资金层正交**：把《顿悟股道》所有买入战法都注册成策略；资金流（L2 大单）只是一层**可开关的正交信号**（过滤/确认/忽略），不砍掉其他战法。
5. **模拟盘优先于实盘**：全程模拟成交、每步留决策建议，攒样本；真实交易由人执行。
6. **一致性约定沿用现有工程**：CK 业务/分析表沿用 `ReplacingMergeTree` + `_ver DateTime MATERIALIZED now()` + 纯 INSERT 幂等；实时热数据走 Redis。金额=元、幅度=%、成交量=手。

---

## 1. 整体闭环架构（7 模块）

```
┌─ T-1 盘后（复用 S1–S8 复盘） ────────────────────────┐
│ ① 盘前计划 plan_pool                                  │
│    三重共振(大势S1+情绪S2+寻龙S4) → 方向+股票+候选战法  │
│    + 触发价/计划仓位/计划动作                          │
│    （对应需求 4.1：基于综合研判给明天关注目标）         │
└───────────────────────┬─────────────────────────────┘
                        │ 用户确认/调整 → 放入关注池
┌─ T 日 盘中（新模块） ──▼─────────────────────────────┐
│ ② 实时接入 ingest：Python爬虫→Redis Stream→后端        │
│    （用户负责：同花顺L2 hexin-v 主源 + pytdx/东财 备源）│
│ ③ 流式计算 stream：内存滚动窗口 → realtime_feature      │
│    （大单净主动买入 / 封单 / 炸板回封 / 放量突破）       │
│ ④ 交易元决策引擎 trader：                              │
│    战法注册表(全量) + 资金层正交信号                   │
│    → 决策建议 action/score/reason/参考价               │
│ ⑤ 模拟盘 sim：按建议+计划仓位模拟成交（每步决策建议）   │
│ ⑥ 前端 SSE 推送（非 WebSocket）：盘中监控 + 决策流 + 模拟盘    │
│    （对应需求 4.2：实时回传+资金确认+按计划模拟买入）    │
└───────────────────────┬─────────────────────────────┘
                        │ T 收盘
┌─ T 盘后（新模块） ─────▼─────────────────────────────┐
│ ⑦ 结算：sim_account/position 算当日收益 → 生成次日卖出 │
│    计划（对应需求 4.3）；并触发 rt_* 每日归档到 CK(回测源头)│
│ ⑧ 经验反馈 exp_log：每笔模拟单+当时L2特征+结果         │
│    → 反哺两个智能体（对应需求 4.4）：                   │
│      · 市场分析师 = 复盘系统(S1–S8) 调参               │
│      · 交易元 = L2 决策 学"哪些真哪些骗"               │
│    （对应需求：数据底座 → 未来量化 agent 消费）          │
└──────────────────────────────────────────────────────┘
```

诚实边界：盘中只能**推断大单**（金额阈值+方向），不能**确认席位身份**（席位仅 T+1 龙虎榜）。故盘中抓"行为痕迹"，盘后龙虎榜"验明正身"——两者互补。

---

## 2. 数据来源与接入契约（L2，源无关）

### 2.1 用户侧负责（本方案不越权）

- **主源**：同花顺 L2（已付费）。`hexin-v` token 由 Node+jsdom 生成、Python 子进程调用、401 自动刷新——**可行**，唯一软肋是 chameleon JS 改版会崩。
- **备源（建议补充，非必须）**：`pytdx`（通达信直连，免费稳定，分笔带 `buyorsell` 方向：0 买/1 卖/2 中性，L1 约 3 秒切片）/ 东财 `push2`（无 token 体操，提供逐笔方向）。token 一崩，管道不至于全断。
- **优先"推"优于"轮询"**：能接推送通道就别高频轮询，延迟更低。

### 2.2 后端接入契约（producer 推来的 JSON，后端只认这个）

爬虫抓取并推送以下三类消息（topic 用 Redis Stream key 区分）：

| 类别 | Stream Key | 字段 | 用途 |
|---|---|---|---|
| 逐笔成交 tick | `rt:tick` | `ts_code, ts(毫秒), price, volume(手), amount(元), direction(0买/1卖/2中性)` | 大单净主动买入、对倒识别 |
| 盘口/行情快照 | `rt:quote` | `ts_code, ts, last_price, pct_chg, amount_day, turnover, high, low, limit_up_price, bid1~10(price,vol), ask1~10(price,vol), seal_amount(封单额)` | 封单变化、炸板回封、扫单 |
| 板块/指数实时 | `rt:sector` | `board_code, ts, pct_chg, lead_ts_code` | 用户提到"板块也可传回"，供板块级共振 |

> 接口层约定：后端消费 Redis Stream，按 `ts_code` 分桶维护内存滚动窗口；崩溃后从 Stream `last-delivered-id` 重放，数据不丢。

### 2.3 中间件标注（显式区分，避免混淆）

- **Redis Streams = 唯一传输总线（全阶段，含 MVP）**：传输 / 缓冲 / **彻底解耦** / 可重放。爬虫只管生产到 Stream，后端 consumer group 消费，二者互不依赖；爬虫崩溃/重启可从 `last-delivered-id` 重放，数据不丢。**WebSocket 直推被否决**（生产/消费强耦合、爬虫崩即丢数据、难重放、难扩展，违背"必须解耦"）。
- **Kafka**：未来扩到全市场/多用户时再上，当前几十只票属 overkill。
- **计算引擎 = 后端自写内存滚动窗口**：每个 `ts_code` 一个 RingBuffer，算近 1/5/15 分钟大单净主动买入额、封单、炸板回封事件。不引入 Flink/Spark Streaming。
- **前端实时展示不用 WebSocket**：后端→前端改用 **SSE（Server-Sent Events）** 单向推送（或短轮询）。后端把 Redis Stream 的消费结果经内存事件总线转发给 SSE 端点，前端与采集链路完全解耦，且不引入 WS。

### 2.4 延迟预算

同花顺 L2 本身 ~1–3s + 抓取周期 + 推送 + 计算 ≈ **秒级滞后**（可达目标，但模拟器抓取有抖动，别追求零延迟）。

---

## 3. 后端新增包结构（在 `com.dunwugudao.replay` 下扩展）

```
replay/
├── plan/        PlanPoolService / PlanGenerator (T-1 计划生成)
├── ingest/      RedisStreamConsumer (消费 rt:*)
├── stream/      TickWindow (RingBuffer) / FeatureCalculator (realtime_feature 派生) / RtArchiveService (每日 Redis→CK 归档, 回测源头)
├── trader/      StrategyRegistry / TraderEngine (决策引擎, 多战法+资金正交)
├── sim/         SimAccountService / SimOrderService / SimTradeService (模拟盘)
├── exp/         ExpLogService (经验反馈落库)
└── (现有) controller/service/entity/job  ── 复用 S1–S8、CkHttpWriter、CkRetryInterceptor
```

新增 REST（前缀沿用 `/api/v1`）：
- `POST /plan/generate?date=` 生成次日计划
- `GET  /plan/pool?date=` 查看关注池
- `SSE  /api/v1/realtime/stream` 盘中监控 + 决策流推送（SSE，非 WebSocket）
- `GET  /sim/account`、`POST /sim/order`、`GET /sim/trades`
- `GET  /exp/stats?groupBy=strategy,stage,capital_confirm` 多口径胜率统计

---

## 4. 表结构设计（核心 · 数据底座）

> 约定：CK 业务/分析表用 `ReplacingMergeTree`，`ORDER BY` 见各表；`_ver DateTime MATERIALIZED now()` 省略标注。
> 实时热数据（tick/quote/feature）**Redis 仅作盘中热缓存**，必须**每日收盘后持久化到 CK 归档层**（plain MergeTree，不可变 append，无需 FINAL）——作为历史回测可溯源的"源头 tape"，Redis 不能长期留存。
> 维度字段（stage / capital_confirm / direction / role / action）一律用**枚举/短码**而非自由文本，保证可 GROUP BY 统计。

### 4.1 盘前计划层

**`plan_pool`**（盘前关注池，CK RMT）
| 字段 | 类型 | 说明 |
|---|---|---|
| plan_date | Date | 计划日期（T-1） |
| ts_code | String | 关注标的 |
| stock_name | String | |
| direction | String | 板块/方向描述（如"AI算力"），用于多口径 direction 维度 |
| board_code | String | 关联板块 |
| role | String( Enum 码: 龙一/龙二/妖/独狼/跟风) | 板位角色 |
| candidate_strategies | Array(String) | 候选战法 id（具体哪招看盘中） |
| planned_action | String | 分歧低吸/一致持有/板上换手/趋势突破 |
| trigger_price | Nullable(Float64) | 触发价 |
| planned_position_pct | Nullable(Float64) | 计划仓位 % |
| note | String | |
ORDER BY (plan_date, ts_code)

**`strategy_catalog`**（战法目录，CK RMT，低频人工/初始化维护）★多战法全量注册
| 字段 | 类型 | 说明 |
|---|---|---|
| strategy_id | String (PK) | 如 `S4_DIVERGE_ABSORB` |
| name | String | 分歧低吸 |
| book_ref | String | S2/S4/S5/S6... |
| applicable_stages | Array(String) | 适用情绪阶段码（见 4.7） |
| board_pos_min / board_pos_max | Nullable(Int32) | 板位要求（如 ≥2 板） |
| capital_role | Enum8('IGNORE'=0,'FILTER'=1,'CONFIRM'=2) | 资金层角色：忽略/过滤/确认 |
| description | String | 战法要点 |
| enabled | UInt8 | 是否启用 |
ORDER BY strategy_id

> 初始化种子（映射《顿悟股道》买入战法）：
> - `S2_ICE_TRY` 冰点试错（适用: 冰点/混沌；capital=IGNORE）
> - `S4_DIVERGE_ABSORB` 分歧低吸（适用: 分歧/分歧转一致；capital=CONFIRM）
> - `S4_CONSENSUS_HOLD` 一致持有不追（适用: 一致/分歧转一致；capital=FILTER）
> - `S4_BOARD_RESEAL` 板上换手回封（适用: 一致；capital=CONFIRM）
> - `S5_LEADER_RELAY` 龙头接力（适用: 启动/分歧转一致；capital=CONFIRM）
> - `S5_HIGH_DIVERGE_SELL` 高位5板分歧卖（适用: 高潮；capital=FILTER）
> - `S6_TREND_BREAK` 趋势突破半仓（适用: 启动/修复；capital=CONFIRM）
> - `S6_TREND_PULLBACK` 沿趋势低吸（适用: 修复/分歧；capital=IGNORE）
> - `S3_MAIN_FORCE_EARLY` 跟主力建仓早（适用: 启动；capital=CONFIRM）
> - `DEMON_DIVERGE_ABSORB` 妖股分歧低吸极限（适用: 分歧；capital=CONFIRM）
> - `WOLF_TREND_LOW` 独狼沿趋势低吸（适用: 修复；capital=IGNORE）

### 4.2 实时层

**`realtime_tick`**（Redis hot + CK plain MergeTree **每日持久化归档（回测源头，必做）**，不可变 append）
| 字段 | 类型 | 说明 |
|---|---|---|
| ts_code | String | |
| ts | DateTime64(3) | 毫秒 |
| price | Float64 | |
| volume | Float64 | 手 |
| amount | Float64 | 元 |
| direction | Enum8('BUY'=0,'SELL'=1,'NEUTRAL'=2) | 主动买/卖/中性 |

**`realtime_quote`**（Redis hash per ts_code，热）
| 字段 | 类型 | 说明 |
|---|---|---|
| ts_code, ts | String, DateTime64(3) | |
| last_price, pct_chg, amount_day, turnover, high, low | Float64 | |
| limit_up_price | Float64 | |
| bid1~10_price/bid1~10_vol, ask1~10_price/ask1~10_vol | Float64 | 十档 |
| seal_amount | Float64 | 涨停封单额 |

**`realtime_feature`**（流式计算结果，Redis hot + CK RMT **连续落库**归档分析）★决策输入
| 字段 | 类型 | 说明 |
|---|---|---|
| trade_date | Date | 交易日（分区键） |
| ts_code | String | |
| ts | DateTime64(3) | 窗口结束 |
| win_minutes | UInt8 | 1/5/15 |
| big_net_buy | Float64 | 大单净主动买入额 元 |
| big_net_buy_ratio | Float64 | 占成交额比 |
| seal_amount | Float64 | 封单额 |
| is_blast | UInt8 | 炸板 |
| is_reseal | UInt8 | 回封 |
| vol_breakout | UInt8 | 放量突破 |
| stage_snapshot | String | 当日情绪阶段码（取自 sentiment） |
ORDER BY (ts_code, ts)

### 4.2.4 每日持久化到 CK（历史回测源头）★

> 需求：Redis 仅作盘中热缓存、不能长期留存；为支持**历史回测找源头**（任意一笔模拟成交/决策都能回溯当时真实 L2 逐笔），盘中热数据必须在**每日收盘后持久化到 CK 归档层**，作为不可篡改的"源头 tape"。

- **持久化时机**：盘后结算（⑦）之前由 `RtArchiveService` 触发。tick 采用 **dual-write**（盘中每笔异步直写 CK 归档，零丢失保证）；feature 本就 CK RMT 连续写入无需额外批处理；收盘后统一 `XTRIM` 清理 Redis Streams。
- **写入方式**：复用现有 `CkHttpWriter`（裸 HTTP 批量直写，异步、幂等）；tick 用 plain MergeTree（不可变 append，无需 FINAL）。
- **归档表**（CK，plain MergeTree，按 `trade_date` 分区）：
  - **`rt_tick_archive`（★真实源头）**：`trade_date Date, ts_code String, ts DateTime64(3), price Float64, volume Float64, amount Float64, direction Enum8`；ORDER BY (ts_code, ts)。承接 Redis `rt:tick` 全量逐笔。
  - **`rt_quote_archive`（盘口降采样）**：结构同 `realtime_quote` + `trade_date`，但**仅按固定间隔（每 5–10s）或事件（炸板/回封）落盘**，避免全量快照撑爆 CK；Redis 仍为盘中完整热数据。
  - `realtime_feature` 已在 4.2.3 连续落 CK RMT，作为派生特征源头。
- **回测关联（核心价值）**：`sim_trade` / `decision_log` 携带 `(ts_code, trade_time)`，可直接 `JOIN rt_tick_archive` 重建触发该笔决策的完整逐笔 tape；`exp_log.entry_features_json` 亦可由 archive 回填校验。**即每一笔模拟交易都可审计、可重放、可归因**。
- **Redis 生命周期**：Streams 保留 ≥1 个交易日（防当日崩溃重放），归档完成后 `XTRIM` 至上限；热 hash（`rt:quote`）设 TTL 或收盘清理。

### 4.3 模拟盘层

**`sim_account`**（模拟账户，CK RMT）
| 字段 | 类型 | 说明 |
|---|---|---|
| account_id | String | |
| trade_date | Date | |
| init_cash | Float64 | 初始资金 |
| cash | Float64 | 可用现金 |
| equity | Float64 | 总资产（市值+现金） |
| position_cost | Float64 | 持仓成本 |
ORDER BY (account_id, trade_date)

**`sim_order`**（模拟委托，CK RMT）
| 字段 | 类型 | 说明 |
|---|---|---|
| order_id | String (UUID) | |
| plan_id | String | 关联 plan_pool（盘中临时可空） |
| ts_code | String | |
| side | Enum8('BUY'=0,'SELL'=1) | |
| price / qty | Float64 | |
| status | Enum8('PENDING'=0,'FILLED'=1,'CANCELLED'=2,'REJECTED'=3) | |
| strategy_id | String | 实际选用战法 ★ |
| stage_at_entry | String | 入场情绪阶段码 ★ |
| capital_confirm | Enum8('NONE'=0,'CONFIRM'=1,'FILTER_PASS'=2,'CONTRA'=3) | 资金层口径 ★ |
| create_time | DateTime64(3) | |
| decision_id | String | 关联 decision_log |
ORDER BY (order_id)

**`sim_trade`**（模拟成交，CK RMT）★**核心统计表**
| 字段 | 类型 | 说明 |
|---|---|---|
| trade_id | String (UUID) | |
| order_id / plan_id | String | |
| ts_code / stock_name / board_code / role | String | |
| side | Enum8('BUY'=0,'SELL'=1) | |
| price / qty / amount | Float64 | |
| trade_time | DateTime64(3) | |
| **strategy_id** | String | ★ 战法口径 |
| **stage_at_entry** | String | ★ 入场情绪阶段码 |
| **capital_confirm** | Enum8 | ★ 资金层口径 |
| entry_reason / exit_reason | String | 买卖理由 |
| planned_action | String | 原计划动作（算 plan_match） |
| pnl | Nullable(Float64) | 卖出笔实现盈亏；买入笔空 |
| d1_ret / d5_ret | Nullable(Float64) | 持有至次日/5日收益 |
| is_trap | Nullable(UInt8) | 是否骗线（高位天量+大单净卖） |
ORDER BY (trade_id)

> **多口径统计 SQL 范式**（后期收窄策略用）：
> ```sql
> SELECT strategy_id, stage_at_entry, capital_confirm,
>        count() AS n,
>        sum(pnl>0 ? 1 : 0)/count() AS win_rate,
>        avg(pnl) AS avg_pnl
> FROM sim_trade FINAL
> WHERE side='SELL' AND pnl IS NOT NULL
> GROUP BY strategy_id, stage_at_entry, capital_confirm
> ORDER BY win_rate DESC, n DESC;
> ```
> 由此得到"哪种 (战法 × 阶段 × 资金口径) 组合挣钱最大"，再把低胜率组合在 `strategy_catalog` 置 `enabled=0` 或调 `capital_role`。

**`sim_position`**（模拟持仓，CK RMT）
| 字段 | 类型 | 说明 |
|---|---|---|
| position_id | String | |
| ts_code | String | |
| qty / avg_cost | Float64 | |
| entry_date | Date | |
| entry_strategy_id / entry_stage / entry_capital_confirm | String/Enum8 | 入场口径（与 sim_trade 一致） |
| last_val_date | Date | |
ORDER BY (position_id)

### 4.4 决策流层

**`decision_log`**（每步决策建议，CK RMT）
| 字段 | 类型 | 说明 |
|---|---|---|
| decision_id | String (UUID) | |
| ts | DateTime64(3) | |
| ts_code | String | |
| context_json | String | 计划+实时特征快照（JSON） |
| action | Enum8('BUY'=0,'SELL'=1,'HOLD'=2,'WATCH'=3) | |
| score | Float64 | 0–100 |
| risk_level | String | |
| reference_price | Nullable(Float64) | 参考价 |
| reason | String | 决策理由 |
| strategy_id / stage / capital_signal | String | 选用的战法/阶段/资金信号 |
| executed | UInt8 | 模拟盘是否执行 |
| order_id | Nullable(String) | |
ORDER BY (decision_id)

### 4.5 经验反馈层（数据底座核心）

**`exp_log`**（经验库，CK RMT）★**未来量化 agent 直接消费**
| 字段 | 类型 | 说明 |
|---|---|---|
| exp_id | String (UUID) | |
| trade_id | String | 关联 sim_trade |
| trade_date | Date | |
| ts_code | String | |
| **strategy_id** | String | ★ |
| **stage** | String | ★ |
| **capital_confirm** | Enum8 | ★ |
| direction | String | 板块方向（多口径维度） |
| role | String | 龙一/妖/独狼... |
| entry_features_json | String | 入场时 L2 特征快照（big_net_buy/seal/blast...） |
| plan_match | UInt8 | 是否按计划执行 |
| outcome_pnl / d1_ret / d5_ret | Float64 | 结果 |
| is_trap | UInt8 | 骗线标记 |
| lesson | String | 经验总结（规则/人工生成） |
| feedback_tag | Array(String) | 标签（"高位天量骗线"/"分歧低吸有效"） |
| consumed_by_agent | UInt8 | 未来量化 agent 是否已消费 |
ORDER BY (exp_id)

> `sim_trade` + `exp_log` 是**数据底座**：干净、可查询、带全维度标签，未来你搭量化 agent 时直接喂。

### 4.6 维度枚举（必须统一，否则无法聚合）

- **stage（情绪阶段码）**：`ICE`(冰点) / `STARTUP`(启动) / `DIVERGE`(分歧) / `DIVERGE_CONSENSUS`(分歧转一致) / `CONSENSUS`(一致) / `REPAIR`(修复) / `CLIMAX`(高潮) / `EBB`(退潮) / `CHAOS`(混沌)
- **capital_confirm**：`NONE`(未用资金层) / `CONFIRM`(资金确认买入) / `FILTER_PASS`(资金过滤通过) / `CONTRA`(资金背离预警)
- **role**：`L1`/`L2`/`L3`/`DEMON`(妖)/`WOLF`(独狼)/`FOLLOW`(跟风)
- **direction**：板块方向字符串（如"AI算力""机器人"）

### 4.7 关于"只做上升期"的处理（回应需求纠偏）

用户明确：**不是预先硬砍成"只做上升期"**，而是前期全战法上、加一层资金判断，最后由数据收窄。
- `strategy_catalog.applicable_stages` 已把每招的适用阶段写成**元数据**（数据驱动），不是全局硬开关；
- 保留一个**可选安全壳**（config 项 `allow_stages` 默认含全部，调试期可临时收窄到上升期相关阶段做护栏），但默认全开；
- 真正"只在对的时机下手"是 `exp_log` 统计跑出来后，把低胜率 `(strategy,stage,capital_confirm)` 组合在 `strategy_catalog` 置 `enabled=0`。

---

## 5. 交易元决策引擎（④ 多战法 + 资金正交信号）

1. **战法注册表**：启动时从 `strategy_catalog` 载入全部 `enabled=1` 战法，每个带 `applicable_stages` + `board_pos` + `capital_role` 元数据。
2. **盘中匹配**：每收到 `realtime_feature`，结合当日 `stage_snapshot` + 标的 `role` + `plan_pool` 候选战法，筛出**当前适用的战法集合**。
3. **资金层正交处理**（按 `capital_role`）：
   - `IGNORE`：资金层不参与；
   - `FILTER`：大单净卖/CONTRA 时否决买入；
   - `CONFIRM`：必须有大单净主动买入才确认信号。
4. **决策输出**：`action(BUY/SELL/HOLD/WATCH) + score(0–100) + risk_level + reference_price + reason + strategy_id + stage + capital_signal`，写入 `decision_log` 并 WS 推送。
5. **模拟盘执行**：`sim_order`/`sim_trade` 按决策 + `plan_pool.planned_position_pct` 模拟成交，每步带 `strategy_id/stage_at_entry/capital_confirm` ★。

---

## 6. 反馈闭环 / 数据底座（回应需求 4.4）

- 每笔模拟单收盘后写 `exp_log`（含入场 L2 特征快照 + 结果 + 骗线标记）；
- **市场分析师智能体** = 复盘系统 S1–S8，消费 `exp_log` 调参（如某阶段判定偏乐观 → 调情绪阈值）；
- **交易元智能体** = L2 决策，消费 `exp_log` 学"哪些 L2 模式是真上车、哪些是骗线"（统计 `entry_features_json` 与 `is_trap` 的相关性，调阈值）；
- 前期是**规则 + 统计反馈**（不是真 AI 自学习），需几十~几百笔样本才有统计意义；攒够后直接升级为 ML/量化 agent 的训练数据——本系统即其**数据底座**。

---

## 7. 分阶段实施路线

| 阶段 | 内容 | 中间件 | 范围 |
|---|---|---|---|
| **M1 MVP** | **Redis Streams 接入** + 后端内存窗口 + 单票小闭环（1 只股票跑通决策+模拟成交） | Redis Streams（必选，全阶段） | 验证决策逻辑 + 解耦管道 |
| **M2** | 引入 Redis Streams（生产/消费解耦、可重放）+ 多票关注池 | Redis Streams | 稳定性 |
| **M3** | 全战法注册（`strategy_catalog` 种子）+ 资金正交信号 + 多口径统计接口 | Redis Streams | 策略广度 |
| **M4** | `exp_log` 反馈闭环 + 前端 3 页（盘前计划/盘中监控+决策流/模拟盘） | Redis Streams + WS | 闭环可用 |
| **M5** | 量化 agent 接入（消费 `sim_trade`+`exp_log`） | — | 数据底座变现 |

> 表结构（第 4 章）在 **M1 即落库**（先建表，后填逻辑），保证后续阶段无缝衔接。

---

## 8. 诚实边界（必须知道）

1. **模拟器/截图 OCR 不稳**：长期建议数据接口直连；源无关设计已兜底可换源。
2. **"交易元越来越厉害"是统计不是魔法**：前期规则+胜率表，样本够多才调得动；几笔无效。
3. **盘中只能推断大单、不能确认席位**：席位 T+1 龙虎榜才知，盘中/盘后互补。
4. **L2 经模拟器有丢帧**：秒级可达但有抖动，别追求零延迟。
5. **"只做上升期"不预硬砍**：由 `exp_log` 数据驱动收窄，预留可选安全壳。

---

## 9. 与现有工程的关系

- 复用：`CkHttpWriter`（裸 HTTP 直写）、`CkRetryInterceptor`（瞬态重试）、双数据源（og@Primary 仅 `replay_calc_task`、ch 全部业务）、S1–S8 计算层。
- 不动：盘后 `ReplayCalcJob`、现有 8 张计算层表。
- 新增：第 3 章 6 个包 + 第 4 章 11 张表（plan_pool/strategy_catalog/realtime_*/sim_*/decision_log/exp_log）+ 新 REST/WS。
- 红线：依旧**别连旧 Linux CK `124.223.220.245`**（过期副本），只连 Windows `100.97.74.45`。

---

## 10. M1 落地记录（2026-08-22，已验证 ✅）

### 10.1 落地内容

| 模块 | 落点 | 状态 |
|---|---|---|
| Redis Streams 消费 | `realtime/RedisStreamConsumer`（consumer group `replay-consumer`，XREADGROUP 阻塞读 rt:tick/rt:quote/rt:sector） | ✅ |
| 内存滚动窗口 | `realtime/TickWindow`（每标的 15min 环形窗口 + 最新盘口） | ✅ |
| 特征计算 | `realtime/FeatureCalculator`（每 3s：1/5min 大单净主动买入、炸板/回封状态机、放量突破） | ✅ |
| 逐笔归档 | `realtime/TickArchiver`（独立线程异步批转行 → CkBufferedWriter） | ✅ |
| 缓冲落库 | `realtime/CkBufferedWriter`（多表攒批、5s flush、失败放回重试、20 万行上限保护） | ✅ |
| 情绪阶段 | `realtime/StageSnapshotHolder`（60s 自动映射 thermal→阶段码 + REST 手动覆盖优先） | ✅ |
| 战法目录 | `trader/DunwuStrategies`（买入 8 条 + 卖出 3 条，id 与 CK strategy_catalog 一致） | ✅ |
| 交易元 | `trader/TraderEngine`（阶段闸门 + 资金三态 CONFIRM/FILTER_PASS/CONTRA + 买入取最高分/卖出风控优先 + 决策限流 30s/60s） | ✅ |
| 模拟盘 | `sim/SimService`（内存账户+持仓，整手成交，每笔落 sim_order/sim_trade/sim_position/sim_account） | ✅ |
| 关注池 | `plan/PlanPoolService`（内存态 + 落 plan_pool，REST 灌入） | ✅ |
| SSE 推送 | `controller/RealtimeController`（`GET /api/v1/realtime/stream`，事件名=FEATURE/DECISION/SIM_TRADE） | ✅ |
| 调度开关 | `config/SchedulingConfig`（@EnableScheduling——此前工程无定时任务，必须显式开） | ✅ |
| CK 11 表 | `docs/ck_ddl_intraday.sql` 已在 Windows CK 执行（12 条语句全 OK，战法种子 11 条入库） | ✅ |

### 10.2 新增 REST 契约

```
POST   /api/v1/plan/pool            灌入/覆盖关注池（数组，落 CK plan_pool）
GET    /api/v1/plan/pool            当前关注池
DELETE /api/v1/plan/pool/{tsCode}   移除单条   DELETE /api/v1/plan/pool 清空
GET    /api/v1/plan/strategies      战法目录（id/名称/方向/适用阶段/资金角色）
GET    /api/v1/realtime/stream      SSE 事件流（FEATURE/DECISION/SIM_TRADE）
GET    /api/v1/realtime/status      实时层状态（阶段/窗口标的/盘口快照/池规模）
GET    /api/v1/realtime/decisions   近期决策流（默认 50，含未执行的 WATCH/HOLD）
GET/POST /api/v1/realtime/stage     查询/手动覆盖情绪阶段（manual 覆盖优先）；POST /realtime/stage/reset 恢复自动
GET    /api/v1/sim/account          账户快照（现金/市值/权益/盈亏）
GET    /api/v1/sim/positions        持仓（实时估值）
GET    /api/v1/sim/trades           成交流（倒序 200 条）
POST   /api/v1/sim/reset            重置账户（可选 initCash）
```

### 10.3 端到端冒烟结论（本地 Redis + Windows CK，2026-08-22）

- **买入链**：XADD 4 笔 60 万大单主动买 + 盘口回调 -3% → 5min 特征 bigNetBuy=480 万（CONFIRM）→ 阶段 DIVERGE 过闸 → `S4_DIVERGE_ABSORB` 触发 BUY（score 80）→ 模拟成交 100 股@1455。
- **持有链**：建仓后特征继续来 → HOLD（限流 60s），无卖出信号不动。
- **卖出链**：阶段覆盖 CLIMAX → `S5_HIGH_DIVERGE_SELL`（"情绪高潮=收割完成，该撤"）→ 全仓卖出 @1460，**pnl=+500 元**，账户清仓回到 1,000,500。
- **零丢失验证**：中途故意杀死后端，重启后 consumer group 从断点**重放积压 tick**（rt_tick_archive 完整收到两批 10 笔），解耦+可重放设计实证有效。
- **CK 落库**：rt_tick_archive=10 / realtime_feature=25 / decision_log=2（BUY executed=1）/ sim_order=1 / sim_trade=1 / sim_position=1 / sim_account=2 / plan_pool=1，全部按 DDL 列入库。

### 10.4 运行方式

```bash
# 1) Redis（开发=本机；生产=Windows 侧，与 CK/OG 同机）
redis-server --daemonize yes            # 或改 REPLAY_REDIS_HOST 指向远程

# 2) 后端（注意沙箱/环境变量可能注入 SERVER__PORT 覆盖 yml，显式指定最稳）
java -jar target/replay-backend-0.1.0.jar --server.port=8090

# 3) 爬虫侧（用户实现，契约）：单字段 payload JSON
redis-cli XADD rt:tick  '*' payload '{"tsCode":"600519","ts":<epoch_ms>,"price":1500,"volume":400,"amount":600000,"direction":0}'
redis-cli XADD rt:quote '*' payload '{"tsCode":"600519","ts":<epoch_ms>,"lastPrice":1455,"pctChg":-3.0,"amountDay":8e9,"turnover":0.5,"high":1520,"low":1450,"limitUpPrice":1617,"sealAmount":0}'
```

### 10.5 已知边界 / 下一步（M2+）

- 收盘后 `XTRIM` 清 Redis Streams 尚未自动化（当前靠 maxlen 手动/自然淘汰）。
- `plan_pool` 重启后不回读 CK（内存态，REST 重灌即可；M2 加回读）。
- 盘后结算（d1/d5 回填、exp_log 生成）未实现——M2 与 ReplayCalcJob 对接。
- exp_log 反馈闭环与前端 3 页（盘中监控/决策流/模拟盘）——M3/M4。
- feature 的 vol_breakout 用「当日分钟均量倍数」近似（非 N 日均量），阈值待经验库校准。

---

## 11. M2 盘后结算 + 收盘 XTRIM（2026-08-22 落地并验证）

### 11.1 新增代码

| 类 / 文件 | 职责 |
|---|---|
| `sim/AfterCloseSettlement.java` | 盘后结算：d1/d5 收益回填 + exp_log 生成 |
| `realtime/StreamTrimmer.java` | Redis Stream 收盘 XTRIM（cron 15:05 + 手动 REST） |
| `mapper/ck/SimTradeCkMapper.java` + XML | 读 sim_trade（按交易日 / 按 ts_code / d1_ret 为 NULL / distinct ts_code） |
| `mapper/ck/SimTradeRow.java` | sim_trade 读模型 |
| `job/ReplayCalcJob.java` | run() 末尾挂 `afterCloseSettlement.settle(tradeDate)` |
| `controller/AdminController.java` | 加 `POST /admin/settle`、`POST /admin/xtrim` |

### 11.2 d1/d5 收益口径

```
d1_ret = (close(T+1) - basis_price) / basis_price × 100
d5_ret = (close(T+5) - basis_price) / basis_price × 100
```
- **basis_price 统一取「买入价」**（关键修正，见 11.7）：sim_trade 的 d1_ret/d5_ret 对 BUY/SELL 笔均回填"买入后持有不动"基准；exp_log 闭环经验也用配对 BUY 笔的买入价算。
  语义=「如果当初买入死拿，到 T+1/T+5 赚多少」——与主动买卖 pnl 横向对比才有意义，且**卖出当天 T+1 尚无数据时用 BUY 基准可立即带值，不必等后续交易日补齐**。
- 数据源：`stock_daily` 真实收盘价（TreeMap 取晚于成交日的第 N 个交易日，自然日升序近似交易日）。
- 回填方式：RMT 重 INSERT 整行（ORDER BY trade_id + _ver 折叠），不 UPDATE。
- T+N 无数据（超出 stock_daily 已有范围）→ d1/d5 留 NULL，正确跳过。

### 11.3 exp_log 生成口径

- 以「卖出闭环 / 买入开仓」为单位配对 BUY→SELL（按 ts_code 升序，openBuy 游标配对）。
- 闭环经验：回溯同 ts_code 最近一笔 BUY 的 entry 信息；开仓经验（仍持仓）：direction=BUY，outcome_pnl=NULL。
- 多口径维度齐备：`strategy_id × stage × capital_confirm × direction × role`。
- `exp_id = "EXP_" + trade_id` → **幂等**（重跑不产生重复经验，已验证：启动 recalc + 手动 settle 两次触发，exp_log 仅 1 条）。

### 11.4 XTRIM 语义

- 盘中 L2 tick 已双写 `rt_tick_archive`（CK 持久化），stream 仅作传输总线，不需保留历史。
- `maxLen` 默认 1000（≈1 小时窗口兜底），收盘 cron `0 5 15 * * ?` 自动裁；手动 `POST /admin/xtrim` 即时裁。
- 验证：rt:tick 1465 条 → xtrim → 1000 条（阈值裁剪生效）。

### 11.5 端到端验证结论（本地 Redis + Windows CK）

- **d1/d5 回填**：造 8-18 BUY + 8-21 SELL（000069.SZ），BUY 笔算出 `d1_ret=-0.565%`（8-19 收盘 1.76 vs 买价 1.77，正确）；SELL 笔 T+1=8-22 尚未产生 → NULL（正确行为）。
- **exp_log 生成**：闭环经验 1 条，多口径维度齐全，lesson 正确（"闭环亏损 0 元：需复盘卖出时机"）。
- **幂等**：exp_log 重跑不重复（FINAL count=1）。
- **XTRIM**：1465→1000 按阈值裁剪生效。
- 启动 `calc.on-startup=true` 时 M2 结算自动挂在 recalc 末尾执行（日志：`盘后结算开始/结束: 2026-08-21`）。

### 11.7 设计瑕疵修复（2026-08-22）：SELL 闭环经验 d1/d5 口径

- **原缺陷**：exp_log 闭环经验的 d1/d5 取 SELL 笔自身价（"卖出后持有"基准），且依赖 SELL 笔 d1_ret 回填——SELL 当天 T+1 无数据 → 落 0。卖出当天的经验无法立即带 d1/d5。
- **修复**：`AfterCloseSettlement.buildExp` 对 SELL 闭环经验统一改用配对 BUY 笔的买入价 + 买入日算 T+N 基准（"买入后持有"基准）。BUY 开仓经验维持自身买价。
- **验证**：造 8-18 BUY + 8-21 SELL，修复后 SELL 经验 `d1_ret=-0.565%`（用买价 1.77，T+1=8-19 收 1.76）；旧逻辑落 0。幂等仍成立（`EXP_`+trade_id 派生，重跑不重复）。
- coding：`buildExp` 新增 basisRow 分支（isClosed→entryBuy，否则 closeOrOpen），closeAtOffset 用 basisDate 计算；类注释口径同步更新。

### 11.6 已知边界

- d5_ret 需成交日后第 5 个交易日收盘数据，盘中/当日成交自然滞后，属预期。
- exp_log.entry_features_json 当前为 `{}`（M3 补"决策前特征快照"，供 agent 归因）。
- plan_pool 重启回读 CK 未做（M2+ 待办，当前内存态 REST 重灌）。
- 前端 3 页（Vue3 接 SSE：盘中监控/决策流/模拟盘）未做（M3）。

## 12. 同花顺 L2 Redis 契约对齐（2026-08-23）

用户给出「同花顺 L2 Redis 数据契约 v1.0」，与 M1 的「三全局流」范式**不兼容**（每股票一个 Stream + Hash 盘口 + Pub/Sub），后端消费层做了重写对齐。

### 12.1 契约 vs 后端模型差异
| 维度 | M1 旧 | 同花顺契约 v1.0 |
|------|-------|----------------|
| Stream 组织 | 3 全局流 `rt:tick/rt:quote/rt:sector`（payload 含 tsCode） | 每股票流 `ths:l2:tick:{code}` / `ths:l2:quote:{code}`，key 含 code |
| tick 字段 | `tsCode/ts/price/volume/amount/direction(int 0/1/2)` | `t/p/v/d/a`，`d`=B/S（字符），`t`=unix-ms 或 HHMMSSmmm（未钉死） |
| quote 结构 | Stream payload（精简版） | **Hash** `ths:l2:quote:{code}`（10 档盘口，60s TTL，覆盖写） |
| 板块 | `rt:sector` 流 | 无板块流（仅 `ths:l2:pool` Set + `ths:l2:status:{code}`） |
| 发现机制 | 固定 3 流 | `SMEMBERS ths:l2:pool` + 订阅 `ths:l2:pool:change`（Pub/Sub）动态增删 |

### 12.2 后端改造（已编译通过 ✅）
1. **Tick POJO**（`model/Tick.java`）：`direction` 改 `String`，加 `DirectionDeserializer` 统一 B/S/0/1/2 → BUY/SELL/NEUTRAL；`ts` 加 `TickTimeDeserializer` 兼容 unix-ms 与 HHMMSSmmm；字段加 `@JsonProperty` 映射 `t/p/v/a`。`FeatureCalculator` 改用 `t.directionSign()`。
2. **Quote POJO**（`model/Quote.java`）：对齐同花顺 key（`last_price→lastPrice`、`prev_close→prevClose`、`amount→amountDay`、`high/low` 同名、`bid1_v×bid1_p→sealAmount`），加封板信息容错。
3. **RedisStreamConsumer**：从「读 3 全局流」改为「`SMEMBERS ths:l2:pool` 发现 → 每 code 一个 XREADGROUP 线程读 `ths:l2:tick:{code}`+`ths:l2:quote:{code}`；订阅 `ths:l2:pool:change` 动态增删；周期 30s 兜底刷新池」。payload 内 tsCode 缺失时用 stream key 的 code 补全。
4. **StreamTrimmer**：从三全局流改为遍历 `ths:l2:pool` 对所有 `ths:l2:tick:{code}` 逐键 XTRIM（maxLen 10000 对齐契约）。
5. **application.yml**：redis host 默认 `100.97.74.45`；stream 段加 `pool-refresh-ms:30000`、`xtrim-max-len:10000`。

### 12.3 关键约定（需爬虫侧遵守）
- **quote 双写**：契约原文 quote 是 Hash（非 Stream）。后端特征引擎吃 Stream，约定**爬虫在写 Hash 的同时额外 `XADD ths:l2:quote:{code} * payload=<完整盘口JSON>`** 喂后端窗口；Hash 留给前端/爬虫直接读。
- **tick 时间格式**：后端 POJO 已兼容 unix-ms 与 HHMMSSmmm 两种，但**强烈建议爬虫用 unix-ms**（解析零歧义）。
- **板块流（可选）**：若爬虫提供板块/指数快照，XADD 到 `ths:l2:sector`（全局流，payload 含 boardCode）即可被后端留存；不提供则后端无板块实时特征（用 T+1 复盘数据补）。
- **股票池**：爬虫通过 `SADD ths:l2:pool {code}` 维护订阅池，变更时 `PUBLISH ths:l2:pool:change '{"action":"add|remove","code":"xxx"}'`。

### 12.4 待办（爬虫侧对齐后）
- 真实 record 样例校验 POJO 字段（尤其 `t` 实际格式、`d` 是否恒为 B/S、盘口字段命名是否完全对齐）。
- M3 拆单识别（万手连单）基于此逐笔流做时间聚合特征，direction 准确性是硬前提。
- 前端 3 页 SSE 字段无需改（后端内部已归一，对外事件结构不变）。

### 12.5 字段映射 bug 修复（2026-08-23 13:xx）
- **Bug**：Tick/Quote 的 `@JsonProperty(value=...)` 误写成**业务字段名**（如 `"ts"/"price"/"volume"/"amount"/"direction"`），而非同花顺契约真实 JSON key（`t/p/v/a/d` 与 `last_price/prev_close/...`）。导致反序列化时 JSON key 全不匹配、所有字段落默认 0/null（用断言测试 `TickParseTest` 复现：BUY/SELL 全部解析为 0）。
- **Fix**：Tick 改 `@JsonProperty("t"/"p"/"v"/"a"/"d")`；Quote 改 `@JsonProperty("timestamp"/"last_price"/"prev_close"/"high"/"low"/"amount"/"limit_up_price"/"bid1_v"/"bid1_p"/"pct_chg")`。Quote 的 `sealAmount` 去掉 Lombok getter（`@Getter(AccessLevel.NONE)`），改为计算属性：显式 `seal_amount>0` 用之，否则用买一档 `bid1_v×bid1_p×100` 估算封单额。
- **验证**：`TickParseTest`（JDK21 + 完整 maven classpath）断言全部 PASS——t=unix-ms 映射、d=B/S→BUY/SELL(+1/-1)、v=int→double、a 缺失→0、quote 全部字段映射 + seal 兜底估算均正确。`mvn -o clean package` 通过。

### 12.6 M3 拆单识别（2026-08-23 14:xx）
- **目标**：从逐笔 tick 流识别主力三类手法——拆单吸筹(STEALTH)、扫单(SWEEP)、对敲(SELF_TRADE)。基于时间聚合特征，非单笔阈值，可识别"万手连单被拆成小单"的隐蔽吸筹。
- **落点**：
  - 新增独立类 `OrderPatternAnalyzer`（静态 `analyze(List<Tick>, cutoff, now, Config)` → `Result(stealthNetBuy, sweepDensity, selfTradeRatio, orderPattern)`），不依赖 Spring，可独立单测。
  - `RealtimeFeature` 加 4 字段：`stealthNetBuy`（拆单净买额，元）、`sweepDensity`（扫单密度 0~1）、`selfTradeRatio`（对敲占比 0~1）、`orderPattern`（NORMAL/STEALTH/SWEEP/SELF_TRADE/MIXED）。
  - `FeatureCalculator` 每 3s 算特征时调用 `OrderPatternAnalyzer.analyze`，注入进 `RealtimeFeature` + `realtime_feature` CK 表（`FEATURE_COLUMNS` 同步加 4 列）。
  - CK DDL（`docs/ck_ddl_intraday.sql` 的 `realtime_feature` 表）加 4 列：`stealth_net_buy Float64 / sweep_density Float64 / self_trade_ratio Float64 / order_pattern String`。
- **算法要点**：
  - **拆单吸筹**：stealth 窗口(默认30s)内，同向(主买 B→+1)且单笔额 < 大单阈值(50万) 的小单累计量破万手当量(10000手) → 命中，`stealthNetBuy` = 这些小单净买额。
  - **扫单**：sweep 窗口(默认10s)内，**价格上行因子主导**（扫单吃卖档推高成交价，priceFactor = min(1, 涨幅×40)；价格平稳→强制 sweepDensity=0，避免与拆单混淆）+ 主买占比/量强度辅助。
  - **对敲**：selfTrade 窗口(默认±200ms)内，量差比例 ≤15% 且方向相反(B/S) 的成对笔 / 总笔数 → ratio。
  - **形态标签**：selfTradeRatio>0.3→SELF_TRADE；sweepDensity>0.6→SWEEP；stealthHit→STEALTH；多命中→MIXED；否则 NORMAL。
- **阈值配置**（application.yml `replay.stream` 段）：`stealth-window-sec:30`、`stealth-min-volume:10000`、`sweep-window-sec:10`、`self-trade-window-ms:200`、`self-trade-vol-tol:0.15`。
- **验证**：`scripts/verify_m3_order_pattern.java`（独立 main + `-ea` 断言，适配离线构建）覆盖 4 类形态全部 PASS——STEALTH(net=2.22E7)、SWEEP(density=0.99)、SELF_TRADE(ratio=1.0)、NORMAL。`mvn -o clean package` 通过。
- **注意**：离线仓库无 `spring-boot-starter-test`，未引入 JUnit；验证以独立脚本固化在 `scripts/`，不污染 `src/test`（避免 `-DskipTests` 离线构建失败）。
- **待办**：① 真实 L2 逐笔数据跑通后校准阈值（尤其 sweep 价格上行敏感度、selfTrade 时间窗）。② 拆单特征接资金三态/新增 S9_STEALTH_ABSORB 战法（M3+）。③ 前端实时页加 orderPattern 展示。
